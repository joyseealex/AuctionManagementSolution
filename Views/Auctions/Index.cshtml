@model List<AuctionsViewModel>

@{
    ViewData["Title"] = "Auction List";
}

<div class="jumbotron">
    <h1 class="text-center">@ViewData["Title"]</h1>
</div>

<table class="table table-bordered">
    <thead class="thead-light">
        <tr>
            <th>
                Description
            </th>
            <th>
                Auction Date
            </th>
            <th>
                <a asp-action="AddOrEditAuctions">Add New Auction</a>
            </th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Count; i++)
        {
        <tr>
            <td>
                @Model[i].Description
            </td>
            <td>
                @Model[i].AuctionDate
            </td>
            <td>
                @Model[i].AuctionItemCount
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@Model[i].AuctionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@Model[i].AuctionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@Model[i].AuctionId">Delete</a>
            </td>
        </tr>
        }
    </tbody>
</table>
